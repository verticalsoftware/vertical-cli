# Customizing flow with middleware

### Overview

The framework uses middleware as an extendable pre-processing pipeline using _chain or responsibility_ patterened components. The components perform the following:

