using System.Text;

namespace Vertical.Cli.Utilities;

internal static class ResponseFileParser
{
    internal static IEnumerable<string> GetArguments(TextReader reader)
    {
        var list = new List<string>(16);
         

        return list.ToArray();
    }
}